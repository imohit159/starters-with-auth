# Next.js inlines NEXT_PUBLIC_* at build time.
# Include the /api/v1 suffix — JSON REST lives under that prefix.
# Prefer the workspace root `.env` via `./setup.sh`.
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
